# 開発メモ(このファイルを更新し続ける。新しいメモは作らない)

**要約:公開中(https://app-takemegane.github.io/english-listen/)。きく・はなす(録音聞き比べ+単語タップで1単語ずつ発音練習)・クイズ・ごほうびガチャまで対応。**

## 修正した点
- 単語音声の質を改善。超スロー生成(速さ120)だと発音がゆがむため自然な速さ(145)+末尾ピリオドで作り直し。「A」は単語単体の標準読み(エイ)を指定。ゆっくり聞きたいときは新設の速度調整を使う方針
- 読み上げ速度の調整を追加(文と単語で別々に ゆっくり/ふつう/はやい。声の高さは変わらない方式)
- 小さすぎた文字を全体的に拡大(日本語訳・バッジ・記録画面など16箇所)
- 録音を全面作り替え。iPhone の録音部品(MediaRecorder)は「1回目は動くが2回目以降壊れる」不具合があるため、マイクの生データを直接ためて直接鳴らす方式(Web Audio 一本化)に変更。修正時に消し忘れた古い参照が録音開始を止めていたバグも除去。教訓:方式を切り替えるときは古い方式の残骸を全部消すこと
- 「マイクが つかえないよ」対策:エラーの種類を画面に表示するようにし、音の通り道を切り替えられる iPhone ではマイクを使い回して確認回数を減らす方式に変更。許可が「しない」になっている場合は iPhone の設定から直す必要がある
- 「自分の声の再生が一瞬で止まる」を修正。録音の一時データを普通の再生機能に渡すと iPhone のホーム画面アプリ形式では止まることがあるため、生データに戻して直接鳴らす方式(Web Audio)に変更。再生中は「◯びょう」と長さも表示し、録音失敗時の案内も追加
- 「はなすモードで自分の声が聞こえない」を修正。iPhone はマイクをつなぎっぱなしにすると通話モードになり、音が耳あてスピーカーから小さく出てしまう。録音が終わるたびにマイクを手放す方式に変更
- 絵本を増やしても端末に「2冊のまま」の古い画面が残る問題を修正。画面や絵本一覧は毎回ネットの最新版を優先して取り、電波がないときだけ保存分を使う方式に変更した(挿絵・音声は今まで通り保存分を優先)。更新後は画面が一度だけ自動で読み直される
- この Mac には高品質な英語の声が入っていなかったため、標準の声(Samantha)で仮の音声を作った。音声はファイルを置き換えるだけで差し替えられる作りにしてある
- 挿絵の表示方法を途中で変更した(ページ内に直接埋め込む方式 → 画像ファイルとして読み込む方式)。オフライン保存の仕組みと相性が良いため

## 機能した点
- はなすモードの録音・聞き比べは「生データ直取り・直再生方式(Web Audio 一本化)」で解決(実機 iPhone で動作確認済み)。iPhone の録音は MediaRecorder を使わないのが正解
- 絵本ナビえいごの公式情報を調べて UX を再現:①絵本を開くと「よみきかせ/クイズ」のモード選択、②読了とクイズ正解でコイン獲得、③コイン3枚でガチャ→どうぶつカード集め(8種)、④きろく画面。クイズの問題文も Ava 音声つき
- iPhone の「最初のタップの瞬間に効果音を無音で一度慣らす」方式(iPhoneはタップなしに新しい音を鳴らせないため。scripts/make_sfx.py で効果音を自作)
- 本棚 → 絵本を開く → 音声再生 → 単語が読み上げに合わせて光る → 自動でページがめくれる、という一連の流れ
- 全ファイル(画面・挿絵・音声)が正しく配信されることをローカルサーバーで確認済み
- 挿絵・アイコンの見た目を画像で確認済み(太陽と本のアイコン、ねこ・おひさまの挿絵)

## 音声の作り直し方(覚え書き)
- Mac に声を追加:システム設定 → アクセシビリティ → リーダーと読み上げ → システムの声の ⓘ
- `bash scripts/generate-audio.sh "Ava (Premium)"` で全ページ再生成
- 音声や挿絵を変えたら sw.js の VERSION の数字を上げる(端末の保存分が入れ替わる)

## まだ入れていない機能(本家との差)
- AI発音採点(単語ごとの色分け評価)は**採用しないと決定**(2026-08-17)。単語タップ練習(お手本+録音聞き比べ)で対応する
- 復習ゲーム/お楽しみ動画/タグ検索/学習カレンダー

## 次にやること
- 使ってみた感想をもとに調整(声の速さ、ボタンの大きさなど)
- さらに絵本を増やす/効果音やごほうび演出の追加
- 直したら git でコミットして GitHub に送ると、公開ページも自動で更新される
- 絵本を増やす(js/books.js にデータ、books/ に挿絵と音声を追加し、sw.js の一覧と版数を更新)
