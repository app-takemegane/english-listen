# 開発メモ(このファイルを更新し続ける。新しいメモは作らない)

**要約:公開中(https://app-takemegane.github.io/english-listen/)。きく・はなす(録音聞き比べ+単語タップでフォニックスカード)・クイズ・ごほうびガチャまで対応。**

## 修正した点
- 「very」を1音ずつ鳴らす聞き比べページを新設(try/very.html + try/very/)。**新データセットの audio/phonemes/*.mp3 は「例単語まるごと」(v→van)で、1音だけの音ではない**ことが判明したため、単独の音を別に作った。作り方は2通り:A案=Piper に発音記号を直接わたして単独合成(`voice.phonemes_to_ids([ipa])` → `phoneme_ids_to_audio`。Ava ではできず「ふつうの英語を読ませる」逃げ方をしていた部分が、新しい声では正攻法で作れる)、B案=単語(van・bed・red・see)をゆっくり合成して切り出し(今のアプリと同じ head/vowel 方式)。**A案の v も Ava と同じ「こすれる音が弱くて低い唸り(ブー)になる」問題が出たので、同じハイシェルフ(1500Hz 以上を +20dB)が必要だった**(教訓:v の弱さは Ava の癖ではなく合成音声に共通の性質)。B案は Piper の子音が短く v が 0.02秒しか取れないため、母音側に 35ms 食い込ませて補正(それでも 0.09秒)。機械測定では新A案の「声の強さ」が4音すべてで Ava を上回る(0.66〜0.95 対 0.31〜0.56)。**ユーザーが「新A案 ゆっくり」を採用。ただし e だけ「たー」に聞こえると報告**→出だしに破裂のような荒れ(高い音の比 0.24)が入っていたのが原因。**Piper は合成のたびに揺らぎ、同じ ɛ でも荒さが 0.03〜0.24 とばらつく**ので、20回作って「荒さ 0.03 以下のうちいちばん長いもの」を選ぶ方式にした(0.24→0.02。承認済みの r と同水準)。あわせて、フェードインの長さが余白と同じで無音部分しか消しておらず立ち上がりが素通しだったバグも修正(余白+0.03秒に変更)。**教訓:合成音声の当たり外れは1回の生成では分からない。出だしの荒さのような数値を決めて、複数回作って選ぶ**。**採用した音をアプリに反映:phonics/r.m4a を新A案ゆっくりの音に差し替え(sw.js v36)**。Piper は合成のたびに揺らぐので作り直さず、**聞き比べで選ばれたファイルそのものを変換して置く**(音量だけ他の音に合わせて RMS 0.170 に調整)。scripts/build-phonics.py に PIPER_SOUNDS を新設し、そこに入れた音は Ava で作り直さない(実行しても上書きされない)ようにした。**教訓:「今の r を直して」は『アプリ側の r を差し替えて』の意味だった。最初これを『新しい音の r を作り直して』と取り違えて長さ違いの候補を5つ作ってしまった。聞き比べページでは『今』『新』がどちらもページ内の候補名なので紛らわしい。どの音を指しているか一言で確認してから作業する**。差し替え後に「変わってない」と言われたが、**今回はサービスワーカーではなく、ふつうのブラウザの記憶(HTTP キャッシュ)が原因**だった(聞き比べページの『今(Ava)』ボタンが同じ phonics/r.m4a を鳴らしていたため、そこで覚えた古い中身が使われていた)。**教訓:同じアドレスのまま中身を差し替えたら、聞き比べページ側もアドレスに版数(?v=36)を付ける。差し替え前の音は比較用に別名で残す(try/very/ava-r.m4a)**。なお公開ページ(GitHub Pages)は push するまで古いままなので、「変わらない」と言われたら ①公開ページ ②ブラウザの記憶 ③サービスワーカー の3つを順に確かめる(md5 で手元と付き合わせるのが確実)。生成は `dataset/.venv/bin/python dataset/build-sample-very.py`(音の名前を引数に付けるとその音だけ作り直す。`--r` で r の候補だけ作る。承認済みの音を揺らがせないため)
- 発音学習用データセットを新設(dataset/)。手作りの88語(js/phonics.js + Ava の m4a)とは別に、**辞書ベースで自動生成する大規模版**を作った。CMUdict-IPA(menelik3/cmudict-ipa)の頻出順リスト上位1000語に対して、発音記号を音素(39音)に分解し、Piper(en_US-lessac-medium)で音声を作って mp3 にする(Chromebook と iPhone の両方で鳴る形式)。分解は「長い記号(tʃ・aɪ・ɑː・ɝː など)から先に照合」+「強く読む印は次の音素に持たせる」方式。**分解した記号をつなげて元の発音記号に戻るかを1語ずつ機械検査**しており、これで ɝː の取りこぼし43語を発見して直した(最終 1000語成功・失敗0)。ファイル名がぶつかる語(IT'S と ITS、A. と A)は番号を足して両方残す。作り直しは `dataset/.venv/bin/python dataset/build-dataset.py`(1000語で約1分)。詳細は dataset/README.md
- フォニックスカードの「v」の音を「ヴー」に修正(ユーザー報告で4回作り直した)。①発音記号の v 単独 → 声が消えて f と同じ「フッ」。②ふつうの英語「vuh.」 → 声は出たが母音(0.23秒)が子音(0.11秒)より長く「ブァ」。③本物の単語「van.」を母音の直前で切る(head 方式を新設)→ まだ「ヴ」ではない。**原因は Ava の v は「こすれる音」が極端に弱いこと**(低いブーンという響きより 30〜35dB も小さく、単独で鳴らすと低い唸りにしか聞こえない)。④そこで 1500Hz から上を 20dB 持ち上げるフィルタ(ハイシェルフ)を head 方式に組み込み、差を 15dB に縮めて解決。立ち上がりも 30ms かけてゆっくりにする(急だと「ブ」と聞こえる)。**教訓:伸ばせる子音は「母音を足す」のではなく「母音の前で切る」+「こすれる音を持ち上げる」。耳で判断することは数値では決められないので、候補を並べた聞き比べページ(try/v.html・try/v2.html)を公開して選んでもらうのが速い**。同じ理由で「z」も無声化していて「ス」に聞こえる(s と数値がほぼ同一)。必要なら同じ方法で直せる
- 音を直しても端末の保存分が入れ替わらない不具合を修正。サービスワーカーの保存(cache.addAll)はSafari の一時保存(GitHub Pages は 10分間有効)から古いファイルを受け取ることがあるため、`new Request(url, { cache: "reload" })` で必ずネットから取り直すようにした(sw.js)。あわせて、ホーム画面アプリは閉じても眠っているだけで読み直されないため、画面に戻るたびに registration.update() で新しい版を確かめるようにした(js/app.js)
- **教訓:音を直しても、git に送る(push)まで iPhone で聞いている公開ページは古いまま。「直したのにまだ変わらない」と言われたら、まず公開ページのファイルを取ってきて手元のファイルと同じか確かめる**
- フォニックスカードに「🐢 1おとずつ → つなげて」ボタンを新設(ユーザー要望:続けて発音するとき少しゆっくり・鳴っている音を強調)。音を順番に少しゆっくり(通常の8割の速さより速くしない。「たんごの はやさ」でゆっくりを選んでいればそちらを優先)鳴らし、鳴っている文字を1つずつ光らせ、音と音のあいだに短い間を置き、最後に単語まるごとを普段の速さで鳴らす。「よまない」文字は飛ばす。速さや間は js/app.js の SOUNDOUT_RATE・SOUNDOUT_GAP・SOUNDOUT_WORD_PAUSE の3定数で調整できる
- 単語タップの音声を「単語単独の正しい読み(辞書の発音)」に変更(ユーザーが以前の「文中の発音そのまま」方式を撤回。a は文中では「ア」だが単独では「エイ」が正しい、など)。ただし a はその後「エイだと文と合わない」とのユーザー要望で「ア」に再変更:文字タップ用の phonics/uh.m4a(ə、実機確認済み)をそのまま流用する COPY_SOUND 方式を build-words.py に追加(合成しないので崩れる心配がなく、文字タップと単語ボタンの音が完全に一致する)。88語の辞書の発音記号(アメリカ英語)を調べて scripts/build-words.py に仕様として記録し、「Blue.」のように1語の文の形で合成(words/ に88語)。音節の数に対して長さが極端な単語を警告する機械点検つき(警告は starts 1件のみ=子音5連続なので自然)。js/books.js の USE_WORD_SOUND を false にすれば以前の clips/ 方式に1行で戻せる(clips/ の生成とオフライン保存は継続中)
- 「勝手に再生しない。押してから再生」に全体を統一(ユーザー要望)。①ページをめくっても読み上げない(再生ボタンを押す。自動めくりモード中の連続読みだけ継続)、②フォニックスカードは開くだけで鳴らさない(文字をタップでその音、ボタンで単語まるごと)、③よみきかせ中の単語タップは「その単語だけ」読んで止まる(前は続きを最後まで読んでいた)。「1もじずつ きく」ボタンは文字タップで足りるため削除
- フォニックスの音の修正:発音記号の単独読みは「破裂する音(g・k)」「すべる音(y)」「形が変わる母音(ow・ay)」で崩れると実機確認で判明。この仲間は全部「ふつうの英語を読ませる」方式に移行(p・b・t・d・ch・j・g → puh 等、k → cuh、w・h・y → wuh 等、ay・ey・oh・ow・oy → Eye・A・Oh・Ow・Oy、ar・or・er → are・or・err)。読ませる英語も油断できない:「kuh」はキュー(Q)、「buh」も誤読された。作りものの英語より、本物の英単語を読ませて末尾の子音を切り落とす方式が最も確実(b は「bud.」の d の直前=破裂の前の一瞬の無音で切って /bʌ/ を得る。TEXT_SYNTH の3つ目に "cut" と書く)。音の質は「母音部分の周波数の重心」をお手本(ʌ=616Hz、uː=535Hz)と比べる方法で機械的に点検できる(キュー読みは451Hzでウー系と判定できた)。w(wuh)は数値がやや低めで要観察。発音記号のまま残っているのは伸ばせる子音と単純母音のみ(実機で問題なしと確認済みの音が中心)。「g」は最初、発音記号の ɡ(特殊文字)が無視されて ə だけ鳴っていた。一息で読む子音のかたまり(sandbox の x、starts・Let's の ts)は2ファイル連結だと「ク…ス」「ト…ス」と間延びするため(ユーザー報告)、1つの音(ks・ts)として新設:本物の単語(box.・cats.)を読ませて破裂の前の無音から後ろだけを切り出す "tail" 方式(bud の "cut" の逆)。母音が残っていないかは周波数の重心で機械点検できる(ks=1268Hz・ts=1641Hz で s=1538Hz と同じ高い仲間、母音 ɑ=627Hz なら失敗)。build-phonics.py も build-words.py と同様に「音の名前を引数に並べるとその音だけ作り直せる」ようにした
- はなすモードの単語タップを「フォニックスカード」に強化。単語を文字のまとまりに分けて表示し(例 live → l・i・v・e、e は「よまない」)、文字をタップするとその音が鳴ってその文字が強調される。発音記号も表示し、母音は赤・子音は青。分け方の辞書は js/phonics.js(全88単語を手作業で定義)、1音ずつの音声は scripts/build-phonics.py で生成(phonics/ に42音)
- 単語タップを「そのページのその位置の発音そのまま」に変更(当時のユーザー要望。→のちに撤回され、上の「単語単独の正しい読み」方式に再変更。words/ フォルダも復活)。ページ音声と単語クリップを同じ合成から作る方式(scripts/build-audio.py、クリップ166件)。副産物として読み上げハイライトが実測時刻(js/timings.js)になり正確化されたのは継続
- 「bird」「flower」の語尾切れを修正。各単語は「後ろに間がある場所(文末・句読点の直後)」を優先して切り出し、語尾の余韻を少し残す方式に改良(88語中45語が間あり位置から取得)
- 単語のお手本を「文の音声からの切り出し」方式に全面変更(blue が単独合成だと何をしても「ビュリュ」に崩れるため)。文を合成しながら各単語の開始時刻を記録し(scripts/speak_with_timings.swift)、その区間を切り出す(scripts/extract-words.py)。単語の音が文中の発音と完全に同一になる。つづり指定で直す旧方式(generate-words.sh)は廃止
- 単語音声の質を改善。超スロー生成(速さ120)だと発音がゆがむため自然な速さ(145)+末尾ピリオドで作り直し。「A」は単語単体の標準読み(エイ)を指定。ゆっくり聞きたいときは新設の速度調整を使う方針
- 読み上げ速度の調整を追加(文と単語で別々に ゆっくり/ふつう/はやい。声の高さは変わらない方式)
- 小さすぎた文字を全体的に拡大(日本語訳・バッジ・記録画面など16箇所)
- 録音を全面作り替え。iPhone の録音部品(MediaRecorder)は「1回目は動くが2回目以降壊れる」不具合があるため、マイクの生データを直接ためて直接鳴らす方式(Web Audio 一本化)に変更。修正時に消し忘れた古い参照が録音開始を止めていたバグも除去。教訓:方式を切り替えるときは古い方式の残骸を全部消すこと
- 「マイクが つかえないよ」対策:エラーの種類を画面に表示するようにし、音の通り道を切り替えられる iPhone ではマイクを使い回して確認回数を減らす方式に変更。許可が「しない」になっている場合は iPhone の設定から直す必要がある
- 「自分の声の再生が一瞬で止まる」を修正。録音の一時データを普通の再生機能に渡すと iPhone のホーム画面アプリ形式では止まることがあるため、生データに戻して直接鳴らす方式(Web Audio)に変更。再生中は「◯びょう」と長さも表示し、録音失敗時の案内も追加
- 「はなすモードで自分の声が聞こえない」を修正。iPhone はマイクをつなぎっぱなしにすると通話モードになり、音が耳あてスピーカーから小さく出てしまう。録音が終わるたびにマイクを手放す方式に変更
- 絵本を増やしても端末に「2冊のまま」の古い画面が残る問題を修正。画面や絵本一覧は毎回ネットの最新版を優先して取り、電波がないときだけ保存分を使う方式に変更した(挿絵・音声は今まで通り保存分を優先)。更新後は画面が一度だけ自動で読み直される
- この Mac には高品質な英語の声が入っていなかったため、標準の声(Samantha)で仮の音声を作った。音声はファイルを置き換えるだけで差し替えられる作りにしてある
- 挿絵の表示方法を途中で変更した(ページ内に直接埋め込む方式 → 画像ファイルとして読み込む方式)。オフライン保存の仕組みと相性が良いため

## 機能した点
- フォニックスの1音ずつの音は「Ava に発音記号(IPA)を直接指定して合成」する方式で作れた(scripts/speak_ipa.swift)。破裂音(p, t, k など)は単独では鳴らないので短い ə を付ける。「r」「ɚ」も単独では無音になるため「rə」「ɜɹ」で代用。「ɡ」(特殊文字 U+0261)は無視されるので普通の g を使う。実機で聞いて不自然だった音は、フォニックス指導の書き方(guh・kuh・yuh など)のふつうの英語を読ませる TEXT_SYNTH 表に移すのが確実。音の追加・調整は build-phonics.py の表を直すだけ
- 単語のお手本の単独合成は「先頭大文字+ピリオド(Blue.)+自然な速さ 0.42」でほぼ崩れないが、blue だけは何をしても「ブリュ」に崩れる(速度が原因ではなく単語固有の癖。長さの機械点検では正常に見えるので耳でしか気づけない)。崩れる単語は「文の終わりに置いた文(It is blue.)を合成して実測時刻で切り出す」方式で解決(build-words.py の EXTRACT_TEXT に文を書くだけ。starts も「Starts.」単独だと語尾の ts が「トス」に崩れたため同方式(It starts.)で解決。語尾の崩れは「音量と周波数の重心を時間を追って見る」点検で機械確認できる=t の無音の後に母音の低い重心が出たら失敗。文末の単語は下がる調子の完全な発音=単独読みとほぼ同じ)。`python3 scripts/build-words.py blue` のように単語名を並べると、その単語だけ作り直せる。ページ音声・クリップは scripts/build-audio.py で再生成できる
- はなすモードの録音・聞き比べは「生データ直取り・直再生方式(Web Audio 一本化)」で解決(実機 iPhone で動作確認済み)。iPhone の録音は MediaRecorder を使わないのが正解
- 絵本ナビえいごの公式情報を調べて UX を再現:①絵本を開くと「よみきかせ/クイズ」のモード選択、②読了とクイズ正解でコイン獲得、③コイン3枚でガチャ→どうぶつカード集め(8種)、④きろく画面。クイズの問題文も Ava 音声つき
- iPhone の「最初のタップの瞬間に効果音を無音で一度慣らす」方式(iPhoneはタップなしに新しい音を鳴らせないため。scripts/make_sfx.py で効果音を自作)
- 本棚 → 絵本を開く → 音声再生 → 単語が読み上げに合わせて光る → 自動でページがめくれる、という一連の流れ
- 全ファイル(画面・挿絵・音声)が正しく配信されることをローカルサーバーで確認済み
- 挿絵・アイコンの見た目を画像で確認済み(太陽と本のアイコン、ねこ・おひさまの挿絵)

## 音声の作り直し方(覚え書き)
- Mac に声を追加:システム設定 → アクセシビリティ → リーダーと読み上げ → システムの声の ⓘ
- `bash scripts/generate-audio.sh "Ava (Premium)"` で全ページ再生成
- 単語を追加したら js/phonics.js に分け方を書き、`python3 scripts/build-phonics.py` を実行(辞書の検査+1音ずつの音声の再生成)
- 単語タップ用の単独読み音声は `python3 scripts/build-words.py`(words/ に生成)。単語を増やしたら同スクリプトの WORD_IPA に辞書の発音を書き足す
- 音声や挿絵を変えたら sw.js の VERSION の数字を上げる(端末の保存分が入れ替わる)

## まだ入れていない機能(本家との差)
- AI発音採点(単語ごとの色分け評価)は**採用しないと決定**(2026-08-17)。単語タップ練習(お手本+録音聞き比べ)で対応する
- 復習ゲーム/お楽しみ動画/タグ検索/学習カレンダー

## 次にやること
- 使ってみた感想をもとに調整(声の速さ、ボタンの大きさなど)
- さらに絵本を増やす/効果音やごほうび演出の追加
- 直したら git でコミットして GitHub に送ると、公開ページも自動で更新される
- 絵本を増やす(js/books.js にデータ、books/ に挿絵と音声を追加し、sw.js の一覧と版数を更新)
